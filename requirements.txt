# ✅ 核心庫
openai>=1.3.9          # 或更新版本，建議 1.11.1 以上，支援 chat + tts 模型
python-dotenv>=1.0.0   # 用來讀取 .env 環境變數
pandas>=1.5.0          # 用於讀取 CSV，版本自由

# ✅ Flask 框架
Flask>=2.1.3
flask-cors>=3.0.10     # 處理 CORS 問題

# ✅ 若用到 Selenium（自動訂票等）
selenium>=4.18.1
webdriver-manager>=4.0.1
undetected-chromedriver>=3.5.5  # 如需反反爬蟲瀏覽器

# ✅ 若使用 Python 3.11
httpx>=0.24.1          # OpenAI SDK 內部依賴，部分雲端部署平台會需要指定

# ✅ Render / Production 用
gunicorn               # 如果日後希望用 gunicorn 啟動 Flask（部署 Render 建議）
